# Disable directory indexes and default file serving
Options -Indexes
DirectoryIndex disabled

# Force all requests to Node.js - don't serve any static files from Apache
RewriteEngine On
RewriteRule ^(.*)$ http://127.0.0.1:%{ENV:PORT}/$1 [P,L]
